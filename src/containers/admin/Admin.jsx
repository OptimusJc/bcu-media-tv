import UploadForm from "../../components/upload/UploadForm";

const Admin = () => {
    return (
        <div className="admin">
            <UploadForm />
        </div>
    );
};

export default Admin;
